.DS_Store
.env
.env.prod
.env.staging
.edgee
node_modules
_archives
_codex*
_claude*
_normal*
_edgee*
_rtk*
# Anchor `report-*` to repo root so it doesn't catch test files like
# src/bench/__tests__/report-swe.test.ts.
/report-*
simulation-*
claude-pro-usage.json
article-*.md
# SWE-bench port outputs (TypeScript bench)
patches/
reports/swe-*.md
reports/swe-*.json
# MCP TSR bench outputs (TypeScript bench)
mcp-runs/
reports/mcp-*.md
reports/mcp-*.json
# Per-user MCP server config — may contain inline auth tokens
mcp-bench.json